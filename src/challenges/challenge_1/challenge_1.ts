
"use strict"

const sum = (a: number, b: number) => {
  let _a = a;
  let _b = b;

  const result = _a + _b;

  return result;
}

const sum1 = sum(1, 1);
const sum2 = sum(2, 3);
const sum3 = sum(5, 5);