
"use strict"

// Write a function that takes two numbers (a and b) as argument
// Sum a and b
// Return the result

const sum = (a: number, b: number) => {
  return a + b;
}

const sum1 = sum(1, 1);
const sum2 = sum(2, 3);
const sum3 = sum(5, 5);